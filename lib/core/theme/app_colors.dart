import 'package:flutter/material.dart';

/// Light government / ministry palette.
abstract final class AppColors {
  static const background = Color(0xFFEEF2F7);
  static const surface = Color(0xFFFFFFFF);
  static const surfaceElevated = Color(0xFFF8FAFC);
  static const border = Color(0xFFCBD5E1);

  static const govNavy = Color(0xFF1E3A5F);
  static const govBlue = Color(0xFF1D4ED8);
  static const govGold = Color(0xFFB45309);

  static const primaryOrange = Color(0xFFB45309);
  static const primaryOrangeDark = Color(0xFF92400E);
  static const accentTeal = Color(0xFF0F766E);
  static const accentTealLight = Color(0xFF0D9488);
  static const accentCyan = Color(0xFF0369A1);

  static const destructive = Color(0xFFDC2626);
  static const warning = Color(0xFFD97706);
  static const warningSurface = Color(0xFFFFFBEB);

  static const textPrimary = Color(0xFF0F172A);
  static const textSecondary = Color(0xFF475569);
  static const textMuted = Color(0xFF64748B);

  static const adminBadge = Color(0xFFB45309);
  static const gradientStart = Color(0xFF1E3A5F);
  static const gradientEnd = Color(0xFF0F766E);
}
